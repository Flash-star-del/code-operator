# code-operator 设计答辩提纲

本文只为已经实现并有测试或真实探针支持的设计提供答辩索引，不把计划项写成现有能力。

## 60 秒架构陈述

code-operator 是我从零实现的单任务 CLI coding agent。ModelClient 按 P0 实测契约调用 `kimi-k3`，只把白名单 assistant 字段交给 AgentLoop；AgentLoop 顺序执行原生 tool calls，用原 ID 回传结构化结果，并由轮次、调用数、失败、重复、上下文和 Ctrl-C 条件终止。ToolRegistry 统一校验六个工具的参数，ExecutionPolicy 在代码层限制真实工作区路径、敏感文件和命令审批；文件覆盖还要求完整读取与哈希未变化，命令始终使用参数数组、`shell=False`、固定 cwd、超时和净化环境。ContextManager 只裁剪最旧完整工具回合，不用模型摘要。测试、脱敏审计、人工审核台账和真实隔离任务共同证明闭环；这些是应用层防护，不是操作系统沙箱。

## 关键取舍

| 取舍 | 目标 | 当前方案 | 代价 | 未选替代方案 |
|---|---|---|---|---|
| 自有核心循环 | 满足独立实现并能解释每个状态 | 自写 ModelClient、AgentLoop、Registry、Policy、ContextManager | 功能少于成熟框架 | Agent SDK/Agent 框架，因核心不可控且题目禁止 |
| 原生工具协议 | 获得稳定 ID 和结构化参数 | P0 验证后使用 Chat Completions `tools/tool_calls` | 绑定已验证供应商契约 | 正文 JSON，解析和伪造风险更高 |
| 回放白名单 | 防止未知元数据污染下一轮 | 只回放 `role/content/tool_calls` | thinking 扩展暂不支持 | 任意字段透传，缺少协议证据 |
| 顺序工具执行 | 保持文件状态、审批和 ID 顺序可复现 | 按 assistant turn 原顺序逐个执行 | 独立读取也不能并行 | 并行执行，需要冲突和取消语义 |
| 四层错误 | 只让模型纠正可纠正错误 | 传输重试、协议停止、参数回灌、本地失败回灌 | 分类代码更多 | 所有错误重试或全部终止，都会掩盖根因 |
| 文件写入保护 | 不覆盖未知或并发变化的内容 | 完整读取授权、哈希复核、唯一替换、原子写入 | 多一次读取，不能模糊替换 | 无条件整文件覆盖，可能破坏用户修改 |
| 命令边界 | 避免 Shell 注入并保留人工控制 | argv、`shell=False`、固定 cwd、deny/ask/allow、超时 | 获批代码仍可产生外部副作用 | 通用 Shell 或宣称沙箱，前者风险大、后者不真实 |
| 凭据隔离 | Key 不进入项目、子进程和审计 | 只读环境变量、子进程环境白名单、递归脱敏 | 某些工具环境需显式补充 | 把 Key 写配置文件或继承完整环境 |
| 上下文保护 | 请求前确定性控制输入预算 | UTF-8 `/3` 粗估、预留输出、按完整回合裁剪 | 不是 tokenizer 上界，可能保守或低估 | 自动摘要，额外成本且不可复现 |
| 终止保险 | 避免死循环和孤儿进程 | 轮次/调用/失败/重复上限与进程树中止 | 可能提前停止仍可恢复的任务 | 无限循环或只靠模型自觉停止 |
| 开源校准 | 学习失败模式而不移植核心 | 固定来源 commit，只转化为自有测试 | 需要维护证据映射 | 复制或翻译第三方 Agent 源码 |
| Eval 口径 | 给出可复核而不过度泛化的证据 | 固定任务、全新工作区、前后测试和真实 usage | 少量样本不能代表总体成功率 | 只展示成功录像或报告未经统计的成功率 |

## 高频追问与回答

### 1. 为什么不用框架？

题目明确禁止 Agent SDK/框架；更重要的是，核心循环、消息配对、工具边界和终止条件必须能逐行解释。运行依赖只有 `httpx`，开发依赖只有 `pytest`。

### 2. 参考过哪些项目？

只在 R0 限定范围内查看了固定 commit 的 OpenAI Codex、Gemini CLI、OpenCode，以及 Claude Code 的公开行为文档；来源、许可证、阅读文件和采用项均在 `REFERENCES.md`。

### 3. 参考与复用的边界是什么？

采用的是“ID 必须配对、输出要截断、循环要有上限”等原则，并把它们转成自有失败测试；第三方生产代码复用为无，没有翻译式移植，也没有 `THIRD_PARTY_NOTICES.md` 例外。

### 4. 如何证明核心代码独立实现？

仓库没有 Agent SDK 或第三方 Agent 依赖；REFERENCES 把每个参考观察映射到自有测试而非源码；提交历史保存 TDD 红绿证据、人工审核范围和核心模块的连续演化。

### 5. 多个 tool call 如何保证一一配对？

解析阶段要求同一轮 ID 非空且唯一；Registry 按原顺序为每个合法调用生成恰好一个结果，包括参数错误、拒绝和超限，回放时使用原 `tool_call_id`。

### 6. 为什么协议字段使用白名单？

P0 只验证了关闭 thinking 时的 `role/content/tool_calls` 回放。`finish_reason`、usage、请求 ID 和未知字段只用于内部统计；未验证字段不进入下一轮，避免供应商扩展改变语义。

### 7. 路径边界如何强制？

所有文件工具先解析真实路径，再用路径组件关系确认仍在 workspace；敏感名、符号链接和 Windows 目录联接逃逸会被拒绝。写入还复核读取哈希。

### 8. 命令审批为什么不是沙箱？

审批只决定是否启动给定 argv，`shell=False` 也只避免 Shell 解析。获批的 Python 或测试仍可能联网、访问工作区外文件或启动子进程，因此文档明确不宣称 OS 沙箱。

### 9. API Key 如何隔离？

Key 只从 `CODE_OPERATOR_API_KEY` 读取，不写仓库；请求正文不含 Key，子进程只继承环境白名单，终端、工具错误和 JSONL 摘要使用统一脱敏器。

### 10. 上下文和输出预算如何处理？

每次请求先为 `max_tokens` 预留空间，再用计划锁定的 UTF-8 字节 `/3` 粗估输入。超预算只删除最旧完整工具回合；系统提示、用户任务和最新执行链仍放不下时返回 `CONTEXT_LIMIT`。

### 11. Ctrl-C 和进程树如何处理？

模型请求、审批和工具执行都捕获用户中止；活动命令使用独立进程组，Windows 当前实现结合控制信号与 `taskkill /T /F` 清理树，返回 `USER_ABORTED` 后不再请求模型。

### 12. Eval 能证明什么，不能证明什么？

它能证明固定项目、提示、模型和当次环境下的闭环结果；不能从三次样本泛化整体成功率，也不能证明跨平台或所有仓库稳定。失败运行必须保留，不能换断言制造成功。

### 13. system prompt 和工具 description 为什么分开？

system prompt 管跨轮流程和诚实完成条件，description 管单个工具的局部语义；安全限制由代码强制。分开可以避免重复事实源，也便于对 schema 和全局规则分别测试。

### 14. 使用 AI 辅助后，如何说明本人设计责任？

AI 可以生成候选实现、测试和文档，但不能代替我确定边界、理解失败原因和批准提交。项目用红灯记录、真实探针、完整暂存差异、`REVIEW_LOG.md` 的逐次人工确认和禁止自动代填审核状态，把最终判断责任留给项目作者；答辩时我应能解释每项取舍、代价和未选方案。

## 证据索引与模拟答辩方法

- 协议：`tests/test_client.py`、`tests/test_probe_protocol.py`。
- 循环与错误：`tests/test_loop.py`、`tests/test_context.py`。
- 文件、路径、命令与凭据：`tests/test_filesystem_tools.py`、`tests/test_policy.py`、`tests/test_command_tool.py`。
- 完整离线闭环和真实任务摘要：`tests/test_integration_fake_model.py`、`docs/evidence/m4-real-task.json`。
- 开源边界和人工审核：`REFERENCES.md`、`REVIEWING.md`、`REVIEW_LOG.md`。

模拟答辩时先在 60 秒内完成架构陈述，再随机抽取至少 12 个问题，在 10-15 分钟内按“结论 -> 代码边界 -> 测试/真实证据 -> 代价”回答。遇到未验证能力必须直接说明限制，不用计划项补全答案。
