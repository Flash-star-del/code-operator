# 人工审核制度

本项目把人工审核作为每个 Git 提交的显式门禁。审核记录用于说明项目作者实际检查了哪些变更、依据什么证据作出结论；它不是第三方独立审计，也不能由自动化工具或 AI 代理代替。

## 基本规则

1. 提交前先暂存计划纳入该提交的全部文件，并展示完整暂存差异、文件范围、测试或真实探针结果、剩余风险和敏感信息扫描结果。
2. 项目作者必须在看到审核材料后明确回复“`<审核 ID> 人工审核通过`”。普通讨论、继续执行、允许提交或允许推送均不等同于人工审核通过。
3. AI 代理不得自行把审核状态写成“通过”，不得代填审核人身份，也不得根据沉默或含糊表达推断批准。
4. 审核通过后，在 `REVIEW_LOG.md` 对应记录中写入结论，并在提交消息中加入 `Human-Review`、`Review-Scope` 和 `Review-Record` trailer。
5. 推送是独立门禁。人工审核通过不授权推送；只有当次明确回复“允许推送”或“可以 push”后才能推送。
6. 已推送提交不得为了补审核记录而改写。若历史提交缺少记录，只能以后续提交如实补记，不得伪造其当时已经审核。

## 每次审核的最低范围

- 变更是否符合题目 PDF、`DESIGN.md` 执行勘误和当前计划。
- 文档声明是否有测试、真实探针或可复现检查支持。
- 是否引入超出范围的依赖、第三方 Agent 代码或未经说明的协议行为。
- 是否存在 API Key、Authorization、真实响应正文、日志、缓存或临时文件泄露。
- 失败测试是否先于功能实现，最终相关测试是否全部通过。
- 已知限制、未验证能力和重新决策条件是否如实记录。

## 提交消息格式

```text
<type>(<scope>): <summary>

Human-Review: approved
Review-Scope: <审核 ID>; full-staged-diff
Review-Record: REVIEW_LOG.md#<锚点>
```

`Reviewed-by:` 只在确有对应自然人完成审核且同意公开其姓名和邮箱时使用。自审记录不会冒充 GitHub Pull Request 的第三方 Approval。SSH/GPG 的 `Verified` 只能证明签名身份，不能单独证明审核质量。

## 本地模板

仓库提供 `.gitmessage`。首次克隆后可执行：

```bash
git config --local commit.template .gitmessage
```

模板用于提醒，不构成强制安全边界；最终以公开台账、提交消息和实际 Git 历史是否一致为准。
